Implemented an intelligent formula generation and validation engine within a Java / Spring Boot application, leveraging advanced LLM orchestration, structured output validation, and dual-layer optimization (RAG and Fine-Tuning) to achieve 99% accuracy in translating natural language to executable mathematical/logical formulas.

Used LangGraph to manage multi-step formula generation.

Built a Retrieval-Augmented Generation (RAG) pipeline to inject relevant schema metadata, variable definitions, and domain-specific constraints

An automated pipeline to fine-tune OpenAI GPT-series models.
